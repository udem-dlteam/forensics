# File: common-setup

# This file contains environment variable settings that are used by
# all the shell scripts.

FORENSICS_DIR="`pwd`"

MAIN_BRANCHES="main master"

BUILD_RESULTS_FIELDS="\"system-name\",\"system-shortname\",\"system-desc\",\"system-url\",\"system-icon\",\"system-setup\",\"commit-name\",\"commit-branch\",\"commit-timestamp\",\"commit-author\",\"commit-desc\",\"commit-url\",\"config-name\",\"config-shortname\",\"config-desc\",\"config-setup\",\"machine-name\",\"machine-shortname\",\"machine-setup\",\"machine-specs\",\"build-timestamp\",\"build-result\""

RUN_CONTEXT_FIELDS="\"system-name\",\"commit-name\",\"config-name\",\"usage-name\",\"usage-setup\",\"machine-name\",\"run-start-timestamp\",\"run-end-timestamp\""

RUN_RESULTS_FIELDS="\"benchmark-name\",\"run-result\""

# Define the PATH to find gcc and related tools (it is best to build a
# specific version of gcc with: "cd compilers;./build-gcc gcc-10.2.0").

PATH=\
$FORENSICS_DIR/compilers/gcc-10.2.0/bin:\
$FORENSICS_DIR/compilers/gambit-4.9.3-1352/bin:\
$FORENSICS_DIR/compilers/bigloo-4.4b/bin:\
$FORENSICS_DIR/compilers/cpython-3.9.0/bin:\
$PATH

# Define which commits to use (the references to dates and times are in
# the format of the "date -d..." command).

DATE1="-2 years"      # starting from 2 years ago
SPACING1="30 days"    # use commits at least 30 days apart

DATE2="-2 months"     # starting from 2 months ago
SPACING2="1 days"     # use commits at least 1 day apart

DATE3="-7 days"       # starting from 7 days ago
SPACING3=""           # use all commits

# Define the frequency of major tasks (which check more things than
# minor tasks).

MAJOR_TASK_PERIOD="1 hours"  # 1 hour between major tasks

MAX_TASKS_PER_BRANCH="2"
